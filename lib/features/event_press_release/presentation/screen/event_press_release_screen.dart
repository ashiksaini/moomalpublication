import 'package:flutter/widgets.dart';

class EventAndPressReleaseScreen extends StatelessWidget {
  const EventAndPressReleaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
