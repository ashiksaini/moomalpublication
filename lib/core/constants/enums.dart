enum CartBtnType {
  addToCart,
  goToCart,
}

enum SortBy {
  popularity,
  averageRating,
  priceHToL,
  priceLToH,
}

enum ProductVariation { ebook, book }

enum Type {
  video,
  latestNews,
  syllabus,
}